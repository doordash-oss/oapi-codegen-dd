openapi: 3.0.0
info:
  title: Merge incompatible types test
  version: 1.0.0
paths:
  /test:
    get:
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MergedObject'
components:
  schemas:
    MergedObject:
      allOf:
        - type: object
          properties:
            name:
              type: string
        - type: array
          items:
            type: string

