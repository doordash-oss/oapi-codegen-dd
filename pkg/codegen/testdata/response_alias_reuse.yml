openapi: "3.0.0"
info:
  title: Test API
  version: "1.0"
paths:
  /zone:
    get:
      operationId: zone
      responses:
        "200":
          $ref: "#/components/responses/Zone"
components:
  schemas:
    Zone:
      type: object
      properties:
        id:
          type: string
  responses:
    Zone:
      description: Zone response
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Zone"
