openapi: "3.0.0"
info:
  version: 1.0.0
  title: Server Example API
  description: Simple API demonstrating server code generation without endpoints

components:
  schemas:
    HealthStatus:
      type: object
      required:
        - status
      properties:
        status:
          type: string
          example: "ok"
